# features

Shared smart feature pages consumed by both `web` and `app`. Adopts the web
implementation as canonical, Angular Material UI, `NavigationManager` for nav,
`@Output()` events for platform-divergent actions. No `@ionic/angular` imports.

This library was generated with [Nx](https://nx.dev).
